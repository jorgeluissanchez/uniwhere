'use strict';
const React = require('react');
const { View } = require('react-native');

const Image = ({ testID, style }) => React.createElement(View, { testID, style });

module.exports = { __esModule: true, Image };
